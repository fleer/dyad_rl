total_episodes: 100000
max_steps: 1000
eval_interval: 1000
eval_episodes: 100
checkpoint_interval: 1000
log_interval: 50
reward_step_penalty: 0
dyad: false
# train_freq and gradient_steps are read from agent config (agent.train_freq, agent.gradient_steps)
