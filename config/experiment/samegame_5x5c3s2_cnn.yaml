# @package _global_
defaults:
  - override /env: samegame_5x5c3s2
  - override /agent: cnn
  - override /training: dqn
experiment_name: exp_cnn_samegame5x5c3s2
agent:
  buffer_size: 500000
  learning_starts: 500000
  exploration_decay: 1000000 # Exponential epsilon decay constant in env steps
device: cuda # auto | cpu | cuda
