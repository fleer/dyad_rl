# @package _global_
defaults:
  - override /env: samegame_5x5c3s2
  - override /agent: mlp
  - override /training: dqn
experiment_name: exp_mlp_samegame5x5c3s2
agent:
  net_arch:
    - 1024
    - 1024
  buffer_size: 500000
  exploration_decay: 2000000 # Exponential epsilon decay constant in env steps
