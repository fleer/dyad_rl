# @package _global_
defaults:
  - /agent@agent_a: mlp   # Agent A: MLP on discrete state
  - /agent@agent_b: cnn   # Agent B: CNN on RGB pixels
  - override /env: netslide_2x3
  - override /training: dyad

experiment_name: dyad
