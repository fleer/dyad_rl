# Optuna sweep config for MLP DQN hyperparameter optimization.
#
# Usage:
#   python experiment.py --multirun --config-name=sweep_mlp
#   python experiment.py --multirun --config-name=sweep_mlp env=netslide_3x3
#
defaults:
  - default
  - override /env: netslide_2x3
  - override /agent: mlp
  - override /training: dqn
  - override hydra/sweeper: optuna
  - _self_

experiment_name: sweep_mlp
training:
  total_episodes: 2000

hydra:
  sweeper:
    sampler:
      _target_: optuna.samplers.TPESampler
      seed: 42
    direction: maximize
    n_trials: 50
    n_jobs: 1
    search_space:
      agent.learning_rate:
        type: float
        low: 1e-6
        high: 1e-3
        log: true
      agent.gamma:
        type: float
        low: 0.95
        high: 0.999
      agent.batch_size:
        type: categorical
        choices: [32, 64, 128]
      agent.hidden_size:
        type: categorical
        choices: [32, 64, 128, 256]
      agent.num_layers:
        type: categorical
        choices: [2, 3, 4]
      agent.tau:
        type: float
        low: 1e-4
        high: 0.05
        log: true
      agent.buffer_size:
        type: categorical
        choices: [10000, 50000, 100000]
